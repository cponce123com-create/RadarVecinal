kind = "web"
previewPath = "/"
title = "Radar Vecinal"
version = "1.0.0"
id = "artifacts/radar-vecinal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20246

[services.development]
run = "pnpm --filter @workspace/radar-vecinal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/radar-vecinal", "run", "build" ]
publicDir = "artifacts/radar-vecinal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20246"
BASE_PATH = "/"
